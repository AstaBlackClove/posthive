{
  "name": "social-scheduler",
  "private": true,
  "version": "0.1.0",
  "license": "AGPL-3.0",
  "scripts": {
    "dev:db": "docker start posthive-pg || docker run -d --name posthive-pg -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:15",
    "dev": "pnpm dev:db && pnpm --parallel --filter api --filter web dev",
    "db:stop": "docker stop posthive-pg",
    "db:start": "docker start posthive-pg",
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter api build && pnpm --filter web build",
    "start": "pnpm --parallel --filter api --filter web start",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "@fullcalendar/core": "^6.1.21"
  }
}
